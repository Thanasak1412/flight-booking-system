const PATH_API = {};

const PATH_AFTER_LOGIN = '/';

const PATH_AUTH = {
  register: '/auth/register',
  login: '/auth/login',
};

const PRIVATE_PATH = {
  homepage: '/',
  booking: '/booking',
};

const API_PATH = {
  register: '/auth/register',
  login: '/auth/login',
};

export { API_PATH, PATH_AFTER_LOGIN, PRIVATE_PATH, PATH_AUTH };
